
  # Create UI from Document

  This is a code bundle for Create UI from Document. The original project is available at https://www.figma.com/design/SfdA0asqBVRfNfYl76h4iO/Create-UI-from-Document.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  